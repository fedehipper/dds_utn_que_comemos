punto 1)

punto 2)

punto 3)


punto 4)

Para este command cree la clase LoguearConsultas,elegi usar el Log4j porque es muy facil y rapido de configurar utilizando propiedades 
del sistema o un archivo de configuraciones, teniendo en cuenta que puede que en un futuro se necesite enviar logs por mail, plasmarlos
en archivos, etc. Decidi usar la configuracion estandar con trace,que es el que se suele utilizar en la etapa de desarrollo,
unicamente mostraria por pantalla el mensaje "MasDe100Resultados" en caso de que una consulta devuelva mas de 100 resultados.




punto 5)

Para este Command, cree la clase MarcarRecetasFavoritas que es en este caso una acción, la cual implementa a
la interfaz Command que tiene el método ejecutar(),luego dentro de MarcarRecetasFavoritas el constructor de la misma
guarda el estado del usuario actual junto con la consulta del mismo, para que luego en el método ejecutar() se use al
usuario para marcar a las recetas de la consulta como favoritas, esto ocurre si el usuario previamente en la creación
de su perfil recibió como parámetro un true, en marcaFavorita. Para que funcione esto en la clase Usuario, esta
la clase marcarRecetasComoFavoritas donde hay un forEach que agrega a todas las recetas de la consulta en el historial, 
luego para realizar esto utilice el método marcarFavorita que se desarrollo en entregas previas de este TP
